[project]
name = "dns-scripts"
version = "0.1.0"
description = "Small utilities for checking DNS records"
readme = "README.md"
requires-python = ">=3.10"
license = "MPL-2.0"
dependencies = [
    "dnspython>=2.6",
]

[tool.uv]
package = false
